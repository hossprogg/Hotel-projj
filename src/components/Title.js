import React from 'react'

export const Title = ({title}) => {//allways props are inside curly brackets
    return (
        <div className="section-title">
            <h4 >{title}</h4>
            <div></div>
        </div>
    )
}
